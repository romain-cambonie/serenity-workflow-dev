name: Validation

on:
  workflow_dispatch:
  push:
    branches:
      - '**'
#      - 'build/*'
#      - 'chore/*'
#      - 'ci/*'
#      - 'docs/*'
#      - 'feat/*'
#      - 'fix/*'
#      - 'perf/*'
#      - 'refactor/*'
#      - 'revert/*'
#      - 'style/*'
#      - 'test/*'
#      - 'release/*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/_build-and-upload-artifact.reusable.yml

  validation-matrix:
    uses: ./.github/workflows/_validation-matrix.reusable.yml